# YouTube Watch Later Downloader Configuration
# Copy this file to .env and customize the values

# YouTube playlist URL to download
# Default: Your Watch Later playlist (requires authentication)
# You can also use any other public or private (with cookies) playlist URL
WATCHLATER_URL=https://www.youtube.com/playlist?list=WL

# Directory where downloaded videos will be saved
# The directory will be created automatically if it doesn't exist
# Default: ./yt_watchlater
OUTPUT_DIR=./yt_watchlater

# Path to the JSON archive file (tracks downloaded videos to prevent duplicates)
# Default: ./yt_watchlater_archive.json
ARCHIVE_JSON=./yt_watchlater_archive.json

# Path to your browser cookies file (REQUIRED for private playlists like Watch Later)
# Export cookies using a browser extension (see README for instructions)
# Leave empty/commented if downloading public playlists
# Default: None
COOKIES_FILE=youtube_cookies.txt

# Webhook notifications (optional)
# Send HTTP POST notifications when videos are successfully downloaded
# Leave WEBHOOK_URL empty/commented to disable webhooks
# Default: None (disabled)

# Webhook endpoint URL (hostname and path, without port)
# Examples:
#   - Local server: http://localhost/webhook
#   - Local network: http://192.168.1.100/api/notify
#   - Discord: https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_TOKEN
#   - Slack: https://hooks.slack.com/services/YOUR/WEBHOOK/URL
WEBHOOK_URL=

# Webhook port (optional, defaults to 80)
# Common ports: 80 (HTTP), 443 (HTTPS), 8080 (dev), 3000 (Node.js)
# Default: 80
WEBHOOK_PORT=80

# Webhook authentication secret (optional)
# If set, includes "Authorization: Bearer YOUR_SECRET" header in requests
# Use this if your webhook endpoint requires authentication
# Default: None
WEBHOOK_SECRET=

# Webhook sends JSON payload on successful download:
# {
#   "video_id": "dQw4w9WgXcQ",
#   "title": "Video Title",
#   "upload_date": "20241015",
#   "download_date": "2024-10-15T14:30:00Z",
#   "filepath": "./yt_watchlater/20241015 Video Title [dQw4w9WgXcQ].mp4"
# }
